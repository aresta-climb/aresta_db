name: Integrador de Pull Requests

on:
  pull_request_review:
    types: [submitted]
  issue_comment:
    types: [created]

jobs:
  integrate:
    # Só roda se for um review 'approved' OU se for um comentário '/approve' (apenas de administradores/colaboradores)
    if: >-
      (github.event_name == 'pull_request_review' && github.event.review.state == 'approved') ||
      (github.event_name == 'issue_comment' && github.event.issue.pull_request && contains(github.event.comment.body, '/approve') && contains(fromJson('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association))
    runs-on: ubuntu-latest
    steps:
      - name: Generate Github App Token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Fetch PR Info
        id: pr-info
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          # Pega o número do PR independente de qual evento acionou
          PR_NUMBER="${{ github.event.pull_request.number || github.event.issue.number }}"
          # Usa o GH CLI para pegar as branches
          BRANCH=$(gh pr view $PR_NUMBER --repo ${{ github.repository }} --json headRefName -q .headRefName)
          BASE_BRANCH=$(gh pr view $PR_NUMBER --repo ${{ github.repository }} --json baseRefName -q .baseRefName)
          
          echo "branch=$BRANCH" >> $GITHUB_OUTPUT
          echo "base_branch=$BASE_BRANCH" >> $GITHUB_OUTPUT
          echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT

      - name: Checkout Repository
        uses: actions/checkout@v7
        with:
          token: ${{ steps.app-token.outputs.token }}
          fetch-depth: 0
          submodules: recursive
          ref: ${{ steps.pr-info.outputs.branch }}
          
      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.13'
          cache: 'pip'
          cache-dependency-path: requirements-deploy.txt

      - name: Install dependencies
        run: pip install -r requirements-deploy.txt
          
      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v47
        with:
          files: database/**
          dir_names: true
          dir_names_max_depth: 2
          # Garante que sempre compara a HEAD do PR com a branch base oficial do PR (ex: origin/main)
          base_sha: origin/${{ steps.pr-info.outputs.base_branch }}
          
      - name: Run Deploy Generated
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          PYTHONPATH=. python scripts/deploy_generated.py --target ${{ steps.changed-files.outputs.all_changed_files }}
          
      - name: Commit and Push
        run: |
          git config user.name "Aresta Bot Integrador"
          git config user.email "bot@aresta.climb"
          git add generated/
          git commit -m "chore: compilação automática para deploy [skip ci]" || echo "Nenhuma alteração para commitar"
          git push
          
      - name: Auto Merge
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          # O GitHub precisa de alguns segundos após o 'git push' para recalcular o estado do PR.
          # Adicionamos um loop de retry para evitar o erro "Pull Request is not mergeable"
          for i in {1..5}; do
            if gh pr merge ${{ steps.pr-info.outputs.pr_number }} --merge --auto --delete-branch; then
              echo "Merge realizado com sucesso!"
              exit 0
            fi
            echo "Aguardando o GitHub processar o PR... Tentativa $i de 5"
            sleep 5
          done
          echo "Falha ao realizar o merge após várias tentativas. Pode haver um conflito real."
          exit 1
