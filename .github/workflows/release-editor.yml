name: Lançamento Editor Aresta

on:
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Tipo de incremento de versão'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
          - custom
        default: 'patch'
      custom_version:
        description: 'Versão customizada (obrigatório se bump_type for custom, ex: 1.0.0)'
        required: false
        type: string
        default: ''
      should_publish:
        description: 'Publicar imediatamente para certificação? (se falso, envia como rascunho com --noCommit)'
        required: true
        type: boolean
        default: true


jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Generate Github App Token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main
          token: ${{ steps.app-token.outputs.token }}
          fetch-depth: 1
          submodules: recursive
          sparse-checkout: |
            .
            tests
            editor
            scripts
            serving
            migracoes
            aresta_api
            coleta_de_betas
            pytest.ini
            conftest.py
            pyproject.toml
            uv.lock
            .python-version

      - name: Setup uv e Python
        uses: astral-sh/setup-uv@v10.0.1
        with:
          version: "latest"
          python-version: "3.13"
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Install Dependencies
        run: |
          uv sync --frozen

      - name: Calcular Versão de Release
        id: versao
        shell: bash
        run: |
          RELEASE_VER=$(uv run editor/release_tools/calculate_release_version.py --tipo "${{ github.event.inputs.bump_type }}" --custom "${{ github.event.inputs.custom_version }}")
          echo "versao=$RELEASE_VER" >> $GITHUB_OUTPUT
        env:
          PYTHONPATH: .


      - name: Configurar Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Injetar Versão Oficial
        run: |
          uv run editor/release_tools/bump_version.py editor/core/version.py ${{ steps.versao.outputs.versao }}
          git commit -am "chore: Lançar versão ${{ steps.versao.outputs.versao }} do Editor Aresta"
          git tag editor-v${{ steps.versao.outputs.versao }}

      - name: Desativar Bloqueio de Falhas do Windows (WerFault)
        shell: pwsh
        run: |
          New-Item -Path "HKLM:\SOFTWARE\Microsoft\Windows\Windows Error Reporting" -Force | Out-Null
          Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\Windows Error Reporting" -Name "DontShowUI" -Value 1
          Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\Windows Error Reporting" -Name "Disabled" -Value 1

      - name: Executar Testes (TDD & Coverage)
        timeout-minutes: 10
        env:
          PYTHONFAULTHANDLER: "1"
          PYTHONUNBUFFERED: "1"
        run: |
          uv run pytest -n 0 -v --durations=10

      - name: Isolar Dependências Exclusivas do Editor
        run: |
          uv sync --frozen --only-group editor

      - name: Build PyInstaller
        run: |
          uv run editor/build.py dist
        env:
          PYTHONPATH: .


      - name: Build MSIX Package
        shell: pwsh
        run: |
          $msixDir = "msix_staging"
          New-Item -ItemType Directory -Force -Path $msixDir
          
          Copy-Item "editor\dist\EditorAresta.exe" -Destination $msixDir
          Copy-Item "editor\msix\AppxManifest.xml" -Destination $msixDir
          Copy-Item "editor\msix\Assets" -Destination "$msixDir\Assets" -Recurse
          
          $version = "${{ steps.versao.outputs.versao }}"
          if ($version -match "^\d+\.\d+\.\d+$") {
              $version = "$version.0"
          }
          $manifestPath = "$msixDir\AppxManifest.xml"
          (Get-Content $manifestPath) -replace 'Version="1\.0\.0\.0"', "Version=`"$version`"" | Set-Content $manifestPath
          
          $makeappx = (Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\makeappx.exe" | Sort-Object FullName -Descending | Select-Object -First 1).FullName
          if (-not $makeappx) {
              Write-Error "makeappx.exe not found"
              exit 1
          }
          
          & $makeappx pack /d $msixDir /p EditorAresta.msix

      - name: Disponibilizar pacote MSIX no GitHub
        uses: actions/upload-artifact@v7
        with:
          name: EditorArestaClimb-Windows-MSIX
          path: EditorAresta.msix
          retention-days: 7

      - name: Setup MS Store CLI
        uses: microsoft/microsoft-store-apppublisher@v1.4

      - name: Publish to MS Store
        shell: pwsh
        run: |
          msstore reconfigure `
            --tenantId ${{ secrets.AZURE_DIRECTORY_TENANT_ID }} `
            --sellerId ${{ secrets.AZURE_SELLER_ID }} `
            --clientId ${{ secrets.AZURE_APPLICATION_CLIENT_ID }} `
            --clientSecret ${{ secrets.AZURE_CLIENT_SECRET }} `
            -vvv

          if ($LASTEXITCODE -ne 0) {
              Write-Error "Falha na autenticação do msstore CLI. Verifique os segredos AZURE_* configurados."
              exit $LASTEXITCODE
          }

          $publishArgs = @("publish", "EditorAresta.msix", "-id", "${{ secrets.AZURE_EDITOR_ARESTA_STORE_ID }}")

          if ("${{ github.event.inputs.should_publish }}" -ne "true") {
              Write-Host "Modo rascunho (--noCommit) ativado. O pacote será enviado sem submeter para certificação."
              $publishArgs += "--noCommit"
          } else {
              Write-Host "Publicação imediata ativada. O pacote será enviado e submetido para certificação."
          }

          & msstore @publishArgs
          if ($LASTEXITCODE -ne 0) {
              Write-Error "Falha na publicação do pacote na Microsoft Store."
              exit $LASTEXITCODE
          }

      - name: Calcular próxima versão -dev
        id: next_dev
        shell: bash
        run: |
          NEXT_VER=$(uv run editor/release_tools/calculate_next_dev.py ${{ steps.versao.outputs.versao }})
          echo "version=$NEXT_VER" >> $GITHUB_OUTPUT
        env:
          PYTHONPATH: .

      - name: Injetar Ciclo de Desenvolvimento
        run: |
          uv run editor/release_tools/bump_version.py editor/core/version.py ${{ steps.next_dev.outputs.version }}
          git commit -am "chore: iniciar ciclo de desenvolvimento do Editor Aresta"
        env:
          PYTHONPATH: .

      - name: Push Repositório
        run: |
          git push origin main
          git push origin editor-v${{ steps.versao.outputs.versao }}


