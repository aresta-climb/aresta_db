## 1. Refatoração do Código

- [x] 1.1 Substituir `field.label` por `field.is_repeated` em `editor/core/protobuf_tree_model.py`
- [x] 1.2 Substituir `field.label` por `field.is_repeated` em `editor/views/widget_editor_dados.py`
- [x] 1.3 Substituir `campo.label` por `campo.is_repeated` em `editor/views/widget_editor_dados.py`

## 2. Validação e Testes

- [x] 2.1 Executar a suíte de testes com `python build.py test`
- [x] 2.2 Validar que todos os testes foram executados com sucesso e os warnings relacionados a `label()` foram completamente eliminados
